import React from 'react';
import LoginAuth from './LoginAuth'
import { shallow } from 'enzyme';

describe('LoginAuth', () => {
  describe('renders Component', () => {
    // const wrapper = shallow(<LoginAuth/>)
    // expect(wrapper).exists
  })

  
})

// renders component
// login button calls function
// functional test - signIn, handleChangeText


'when typimg etc, validation appears'
'when pressing submit, thing is called. '
'when submit and make an aysnc call calls valiudatopm'
'if successful call success'